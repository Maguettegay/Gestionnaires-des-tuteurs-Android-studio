<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uvs.ida.gestiontuteurs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".tuteur.AddTuteurActivity"></activity>
        <activity android:name=".chef_filiere.AddChefFiliereActivity" />
        <activity android:name=".cours.AddCoursActivity" />
        <activity android:name=".filiere.AddFiliereActivity" />
        <activity android:name=".cours.CoursActivity" />
        <activity android:name=".chef_filiere.ChefFiliereActivity" />
        <activity android:name=".filiere.FiliereActivity" />
        <activity android:name=".tuteur.TuteurActivity" />
        <activity android:name=".user.UserActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".security.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>